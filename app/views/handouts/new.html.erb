<h1 class="text-3xl font-bold mb-6">Create a New Handout for <%= @mission.title %></h1>

<div class="bg-white p-6 rounded-lg shadow-md border border-gray-200">
  <%= render 'form', mission: @mission, handout: @handout %>
</div>

<div class="mt-6">
  <%= link_to 'Back to Mission', mission_path(@mission), class: 'text-blue-500 hover:underline' %>
</div>
