<ul class="space-y-4">
  <% if character_sheets.any? %>
    <% character_sheets.each do |character_sheet| %>
      <%= render partial: "missions/character_sheet_details", locals: { character_sheet: character_sheet, mission: @mission } %>
    <% end %>
  <% end %>
</ul>
