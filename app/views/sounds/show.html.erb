<h1 class="text-3xl font-bold mb-4"><%= @sound.title %></h1>

<p class="mb-4"><%= @sound.description %></p>

<div class="mb-6">
  <%= audio_tag @sound.file, controls: true, class: "w-full" %>
</div>

<div class="flex justify-end space-x-2">
  <%= link_to "Back to Sounds", sounds_path, class: "text-blue-500 hover:underline" %>
</div>
