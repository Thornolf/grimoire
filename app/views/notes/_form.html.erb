<%= form_with(model: [mission, note], local: false, data: { controller: "autosave", action: "turbo:submit-end->autosave#reset" }) do |form| %>
  <div class="mb-6">
    <%= form.rich_text_area :content, value: note.content, class: "border-gray-300 rounded w-full p-2", placeholder: "Write your note here..." %>
  </div>
<% end %>
